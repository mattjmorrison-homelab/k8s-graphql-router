apiVersion: v1
kind: ConfigMap
metadata:
  name: graphql-router-config
  namespace: graphql-router
data:
  router.yaml: |
    supergraph:
      listen: 0.0.0.0:4000
      introspection: true
    include_subgraph_errors:
      all: true
    cors:
      policies:
        - origins:
            - https://hdmi-switch.morrisons.site
          methods:
            - GET
            - POST
            - OPTIONS
          allow_headers:
            - content-type
    sandbox:
      enabled: true
    homepage:
      enabled: false
    health_check:
      listen: 0.0.0.0:8088
      enabled: true

  supergraph.graphql: |
    schema @link(url: "https://specs.apollo.dev/link/v1.0") @link(url: "https://specs.apollo.dev/join/v0.3", for: EXECUTION) {
      query: Query
      mutation: Mutation
    }

    directive @link(url: String, as: String, for: link__Purpose, import: [link__Import]) repeatable on SCHEMA

    directive @join__graph(name: String!, url: String!) on ENUM_VALUE

    directive @join__type(graph: join__Graph!, key: join__FieldSet, extension: Boolean! = false, resolvable: Boolean! = true, isInterfaceObject: Boolean! = false) repeatable on OBJECT | INTERFACE | UNION | ENUM | INPUT_OBJECT | SCALAR

    directive @join__field(graph: join__Graph, requires: join__FieldSet, provides: join__FieldSet, type: String, external: Boolean, override: String, usedOverridden: Boolean) repeatable on FIELD_DEFINITION | INPUT_FIELD_DEFINITION

    directive @join__implements(graph: join__Graph!, interface: String!) repeatable on OBJECT | INTERFACE

    directive @join__unionMember(graph: join__Graph!, member: String!) repeatable on UNION

    directive @join__enumValue(graph: join__Graph!) repeatable on ENUM_VALUE

    enum link__Purpose {
      """
      `SECURITY` features provide metadata necessary to securely resolve fields.
      """
      SECURITY
      """
      `EXECUTION` features provide metadata necessary for operation execution.
      """
      EXECUTION
    }

    scalar link__Import

    enum join__Graph {
      HDMI_SWITCH @join__graph(name: "hdmi-switch", url: "https://graph-hdmi-switch.morrisons.site/graphql")
    }

    scalar join__FieldSet

    enum HdmiInput @join__type(graph: HDMI_SWITCH) {
      GOOGLE_TV @join__enumValue(graph: HDMI_SWITCH)
      APPLE_TV @join__enumValue(graph: HDMI_SWITCH)
      PS3 @join__enumValue(graph: HDMI_SWITCH)
      PS4 @join__enumValue(graph: HDMI_SWITCH)
      SWITCH @join__enumValue(graph: HDMI_SWITCH)
    }

    type Mutation @join__type(graph: HDMI_SWITCH) {
      setInput(input: HdmiInput!): HdmiInput!
    }

    type Query @join__type(graph: HDMI_SWITCH) {
      currentInput: HdmiInput!
    }
